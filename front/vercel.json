{
  "framework": "nextjs",
  "settings": {
    "buildCommand": "cd .. && cd front && npm run build",
    "devCommand": "cd .. && cd front && npm run dev",
    "installCommand": "cd .. && cd front && npm install"
  }
}